# Gradle / Kotlin
.gradle/
.kotlin/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# Xcode / iOS
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# Secrets — app holds only the public Worker URL; never commit real secrets
.env
.env.*
!.env.example

# IDE
.idea/
*.iml

# Android release signing — NEVER commit these (public repo).
# Bare name so it is ignored wherever it is put, not just the one expected path.
keystore.properties
androidApp/keystore/
*.jks
*.keystore
